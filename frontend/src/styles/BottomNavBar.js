import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: "row",
    justifyContent: "space-around",
    alignItems: "center",
    backgroundColor: "#0e0d16",
    paddingVertical: 12,
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    borderTopWidth: 1,
    height: 69,
    borderTopColor: "#292840",
  },
  navItem: {
    alignItems: "center",
    justifyContent: "center",
  },
});
